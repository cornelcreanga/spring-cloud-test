info:
  component: Config Samples
endpoints:
  restart:
    enabled: true
logging:
  levels:
    org.springframework.boot.env.PropertySourcesLoader: TRACE
    org.springframework.web: DEBUG
    com.ccreanga.springcloud: DEBUG
server:
  port: 8090

sms:
  value:  SMS_DEFAULT_INITIAL_VALUE

debug: true
